## How to Load Tournament Data

1. Download both `index.html` and `tournament-data.json`
2. Open `index.html` in your browser
3. Go to "Data Management" tab
4. Open `tournament-data.json` and copy all content
5. Paste into the import textarea
6. Click "JSON-ის იმპორტი" (Import JSON)
